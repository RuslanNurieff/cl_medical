[project]
name = "cl-medical"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "moviad",
    "ruff>=0.14.14",
    "timm>=1.0.24",
]

[tool.uv.sources]
moviad = { path = "../moviad", editable = true }
